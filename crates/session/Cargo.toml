[package]
name = "bookrack-session"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# The knob-origin vocabulary the runtime-directory row is reported
# through.
bookrack-core = { path = "../core" }
eyre.workspace = true
dirs.workspace = true
fs2.workspace = true

[dev-dependencies]
tempfile.workspace = true
